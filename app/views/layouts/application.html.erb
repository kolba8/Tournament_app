<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Tournaments" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>

    <%= javascript_include_tag "application" %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->
  </head>
  <body>

    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="<%= root_path %>">Tournaments</a>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <li><% if user_signed_in? %>
              <p></p>logged as <strong><%= current_user.email %></strong>
            <% else %>
              <%= link_to "Sign in", new_user_session_path %>
            <% end %></li>
            <li><% if user_signed_in? %>
              <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
            <% end %></li>
            <li><%= link_to "Tournaments", tournaments_path  %></li>
            <li><%= link_to "Teams", teams_path  %></li>
            <li><%= link_to "Hall of fame", best_teams_path  %></li>
          </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-lg-9">
          <!-- <% if user_signed_in? %>
            You are now logged as <strong><%= current_user.email %></strong>
            <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
          <% else %>
            <%= link_to "Sign in", new_user_session_path %> |
            <%= link_to "Register", new_user_registration_path %>
          <% end %> -->

          <!-- <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p> -->
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
        <div class="col-lg-3">
          <div class="well sidebar-nav">
            <h3>Menu</h3>
            <ul class="nav nav-list">
              <li class="nav-header">Pages:<p></p></li>
              <li><strong><%= link_to "Tournaments", tournaments_path  %></strong></li>
              <li><%= link_to "-new", new_tournament_path  %></li>
              <li><strong><%= link_to "Teams", teams_path  %></strong></li>
              <li><%= link_to "-new", new_team_path  %></li>
              <li><strong><%= link_to "Hall of fame", best_teams_path  %></strong></li>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2017</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
